{
  "name": "@the-drop/infra",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "format:check": "prettier --check . --ignore-path ../../.prettierignore",
    "typecheck": "tsc --noEmit",
    "test": "echo \"@the-drop/infra: no tests\" && exit 0",
    "synth": "cdk synth",
    "diff": "cdk diff"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.164.1",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "aws-cdk": "^2.164.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
