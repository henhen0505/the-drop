# Copy to apps/api/.env for local development.

# Runtime
NODE_ENV=development
PORT=3000

# Database (matches docker-compose.yml local Postgres; host port 5433, see docker-compose.yml comment)
DATABASE_URL=postgresql://dev:dev@localhost:5433/thedrop

# Auth
JWT_SECRET=dev-secret-change-me-in-production-32chars
